import { combineReducers } from "redux";

export default combineReducers({
  podcastList,
  blogPostList,
  seletectBlogpost,
  signIn,
  imageList,
  selectedImg,
  listLinkRead,
  researchBlogpostList
});
